import React from 'react';
import ReactDOM from 'react-dom/client';
import { MotionCaptureApp } from './components/MotionCaptureApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MotionCaptureApp />
  </React.StrictMode>
);